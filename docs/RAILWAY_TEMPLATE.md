# Railway deploy: estado actual y upgrade path

## Cómo está hoy (botón funcional)

El README y `docs/CLAUDE_AI.md` tienen un botón "Deploy on Railway" que apunta a:

```
https://railway.com/new/template?template=https%3A%2F%2Fgithub.com%2FSoysebalopez%2Ffelisa
```

Railway clona el repo, detecta `railway.toml` y crea el servicio del MCP server. El usuario después tiene que:

1. Agregar Postgres manualmente (UI: `+ New → Database → PostgreSQL`).
2. Vincular `DATABASE_URL` como reference variable.
3. Setear `ANTHROPIC_API_KEY`, `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_API_TOKEN`, `FELISA_API_TOKEN`, `MCP_PUBLIC_URL`.
4. Re-deploy.
5. Aplicar el SQL inicial vía la UI del Postgres o `railway connect`.

Está documentado paso a paso en `docs/CLAUDE_AI.md`. **Funciona, pero requiere ~5 minutos de setup manual.**

## Upgrade path: template "oficial" de Railway

Si querés que el botón sea más slick (un click → Postgres + servicio + env vars autogenerated → solo pegar las claves):

1. Ir al proyecto **Felisa** en railway.com.
2. `Settings → Templates → Generate Template`.
3. Configurar cada variable:
   - `ANTHROPIC_API_KEY`: required, sin default, descripción + link a console.anthropic.com.
   - `CLOUDFLARE_ACCOUNT_ID`: required, descripción + link a dash.cloudflare.com.
   - `CLOUDFLARE_API_TOKEN`: required, descripción + link a dash.cloudflare.com/profile/api-tokens (con scope Workers AI Edit).
   - `FELISA_API_TOKEN`: autogenerated con `${{secret(32)}}`.
   - `MCP_PUBLIC_URL`: required, descripción "URL pública del servicio (la copia de Railway después del primer deploy)".
   - `DATABASE_URL`: linkeado al servicio Postgres del template.
4. **Publish as public template**. Railway te da una URL tipo `https://railway.com/template/abcdef`.
5. Reemplazar el placeholder en `README.md` + `docs/CLAUDE_AI.md`:
   ```diff
   - https://railway.com/new/template?template=https%3A%2F%2Fgithub.com%2FSoysebalopez%2Ffelisa
   + https://railway.com/template/<id-real>
   ```

Beneficios del template oficial vs el botón actual:

| | Botón actual (deploy from repo) | Template oficial |
|---|---|---|
| Postgres incluido | No, lo agrega el user | Sí, pre-configurado |
| `FELISA_API_TOKEN` | Pegado a mano | Autogenerated |
| `MCP_PUBLIC_URL` | Pegado a mano post-deploy | Idem (es inherente a Railway) |
| Discovery en Railway marketplace | No aparece | Sí, indexado |
| Setup total para el user | ~5 min | ~2 min |

## Healthcheck endpoint

`railway.toml` apunta a `/health` pero ese endpoint no existe en `felisa/mcp/server.py` todavía. Railway puede reportar "no healthcheck" pero el servicio igual funciona. Para taparlo, agregar en `server.py`:

```python
@app.route("/health")
async def health(request):
    return JSONResponse({"status": "ok"})
```

(Está como follow-up; no bloqueante.)

## Schema initial

Idempotente (`create table if not exists`, `on conflict do nothing`). Aplicarlo varias veces no rompe nada — útil porque el usuario lo va a aplicar después del primer deploy.
